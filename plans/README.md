# Implementation Plans

Generated by the improve skill on 2026-09-03 (deep audit, all nine categories, against commit `ff604ab`). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

> **Selection note (non-interactive run).** This audit ran without a user available to choose findings, so per the skill's default the top **5** findings by leverage (impact ÷ effort, weighted by confidence; security findings float) were turned into plans. Every security-critical plan candidate was re-verified line-by-line against the cited code before planning. The next candidates are listed under "Unselected top findings".

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fail closed on unparseable `WITH` headers (relation allowlist bypass) | P1 | S | — | DONE |
| 002  | Quote engine identifiers at DDL assembly sites (gate bypass behind the seam; CSV-header injection) | P1 | S | — | DONE |
| 003  | Alias-proof sensitive release gate (identifier provenance + reassembling aggregates) | P1 | M | 001 (sequencing: both edit `sql-inspector.ts`) | DONE |
| 004  | Guard the analysis commit section — dispatch always settles with an envelope | P1 | S | — | DONE |
| 005  | Arm egress monitoring once across boot retries | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

### Execution notes (2026-09-03, all five plans on one branch)

- **003**: `ANY_VALUE`/`ARBITRARY` also joined `AGGREGATE_WORDS` — without them an `ANY_VALUE(x) … GROUP BY` statement passes the aggregate gate before the reassembling denial can fire. The store redaction contract now binds the sensitive-classified `diagnosis` column: a direct-identifier (`mrn`)-bound analysis is denied by the provenance rule, so it can no longer demonstrate downstream redaction.
- **004**: the non-finite trigger now commits as an honest null KPI (Step 1's coercion), so its regression case asserts that instead of `INTERNAL_ERROR`; the commit guard itself is exercised by a 100-char alias overflowing `ArtifactSummarySchema`'s KPI column bound (80).
- **002**: the pinned `buildIntakeSql` DDL string in `intake.test.ts` gained the quoted relation name — an internal-statement assertion, not user-visible output.
- Environment: `pnpm test` also collects a locked leftover worktree (`.claude/worktrees/deploy-main-pages`), doubling the suite; and three `live-canvas` UI tests fail at the planning commit `ff604ab` in this environment (verified on a clean tree) — both pre-existing, unrelated to these plans.

## Dependency notes

- **003 after 001**: both modify `src/dataset-custody/sql-inspector.ts` (001 in `checkRelations`, 003 in the token loop + `SqlInspection`). Landing 001 first avoids textual conflicts; there is no logical dependency.
- 002, 004, 005 are independent of everything else and can proceed in parallel on separate branches.
- All five plans are safe to execute independently of the untracked working-tree state; each drift-checks against `ff604ab`.

## Verification gates (repo floor, applies to every plan)

```sh
export PATH="$HOME/.nvm/versions/node/v26.8.1/bin:$PATH"   # engines: node ^26
pnpm lint && pnpm lint:strict && pnpm typecheck && pnpm test
```

Baseline at planning time: all four green (typecheck 0.9s, lint 0 warnings, 325/325 tests, docs:parity green).

## Unselected top findings (next candidates, in leverage order)

- **PERF-01** — `materialize` inserts up to 10,000 rows via per-row awaited `insert.query(...)` round trips (`duckdb.worker.ts:125-133`, same in `node-duckdb.ts`); batch to multi-row `VALUES` or a columnar insert. User-perceived latency on every committed analysis.
- **CORRECT-05** — successful imports never `dropFile` the registered CSV buffer (`duckdb.worker.ts:147-166` drops only on the error path); every import retains its full size in the worker's virtual FS until respawn.
- **CORRECT-04** — native `registerTool` failures are swallowed by an empty catch and fall through to the simulator while already-registered tools stay live (`registration.ts:194-216`); half-native surface + lying `simulator_only` capability.
- **CORRECT-08** — `BUDGET_EXCEEDED` is in `DETERMINISTIC_CODES` (`store.ts:154-161`) although the engine marks it `retryable: true`; an exact resend of a deadline-missed query replays the cached failure forever.
- **TEST-01** — only ~2 of 33 `FORBIDDEN_WORDS` have mid-statement deny tests; one `it.each` table closes it (natural second commit on plan 001's file).
- **DOC-02** — canvas vocabulary split: five live tabs (Charts/Query/Rows/SQL & Lineage/Zero Upload) vs "four views (Insights/Data Grid/…/Custody)" still canonical in PRODUCT.md, CONTEXT.md, ARCHITECTURE.md, DESIGN.md, README, video-script, and prd.md itself.
- **DX-01/PERF-07** — CI downloads the 72 MB DuckDB assets in three jobs with no cache; the deploy job ships bytes the e2e job never tested (share one build artifact).
- **SEC-04/05/06/08**, **CORRECT-02..15 (rest)**, **DEBT-01..12**, **DIR-01..06** — see the audit report; several DEBT items are uncommitted-code follow-ups already noted inside plans 004/005.

## Findings considered and rejected

- **DEP-01 (lockfile violates pnpm release-age floor; "CI red today")**: downgraded to investigate — `pnpm install --frozen-lockfile --dry-run` passes cleanly at planning time; the failure two subagents observed was transient (pnpm pre-run verify against a stale `node_modules`). The commented-out floors in `pnpm-workspace.yaml:19-23` remain worth a deliberate decision (see also DEBT-10).
- **Boot failure = blank page** (`main.tsx`): deliberate, documented retry semantics.
- **`fetch(Request)`-body under-accounting**: pinned as an operational-evidence limitation; tracked as SEC-04, not silently accepted.
- **`rawSensitiveValuesReleasedToTools` never increments**: correct today (`rawRowsToAgent` is `z.literal(0)`).
- **Engine tests run without a worker (fake decisions)**: deliberate seam — each custody rule written and tested once.
- **Replacing the Radix select with a native element** (DEP-02/DEBT-05): a real decision for the maintainer (it partially unwinds commit `aad1102`'s primitive-free rationale), not an advisor call. Record the decision either way.
- **TypeScript 7 × Vite 8 compatibility, wrangler currency, react-grab in prod**: no real cost found (dev-guarded behind `import.meta.env.DEV`); not worth doing.
- **Extracting the four evidence views' empty-state scaffolding**: bespoke artwork per view; parameterization would save nothing.
- **Ghost tiles / KPI-chip render-site extraction**: below the 3+ duplication threshold.
- **Coverage tooling (DX-12)**: gap is real; whether the repo wants a %-coverage culture is a maintainer call — report-only wiring is cheap if wanted.
- **ADR 0006**: do not fabricate a retroactive ADR; repoint the dangling references (DOC-03/DEBT-06) at ADR 0001 Amendment 3 / AgDR-0001.
- **Direction items DIR-01..06** (artifact chain view, copy-safe-summary, simulator agent pass, import review moment, release-proof command, multi-artifact ruling spike): grounded options for the maintainer — deliberately not planned; strategy belongs to the owner.
