/** Data Grid evidence view — empty state per the shell decisions (ticket 06). */
export function DataGridView() {
  return <p>No artifact — the grid paints rows only from an approved artifact.</p>;
}
